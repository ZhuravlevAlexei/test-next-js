import React from 'react';
import Header from '@/app/components/header';

export interface PageProps {}

// header для всіх компаній
export default function Page({}: PageProps) {
  return <Header>Companies</Header>;
}
