import React from 'react';

export default function Default() {
  // В React возвращение null фактически ничего не отображает в DOM, что используется для условного отображения компонента
  return null;
}
